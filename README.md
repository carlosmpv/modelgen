# modelgen
Golang struct generation based on postgres databases

# How to use
* run **go install** on cloned directory
* run modelgen --host=<hostname> --port=<port> --dbname=<database name> --password=<database password> --user=<user> --path=<where models will be generated>
* the target path must be named **models** 
  
